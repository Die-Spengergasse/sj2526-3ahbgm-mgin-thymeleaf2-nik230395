# Verwaltungsprogramm mit Web-Oberfläche
### Erstelle analog zur Aufgabe 7 ein Verwaltungsprogramm für Objekte deiner Wahl. Die Objekte werden in einer MySQL-Datenbank gespeichert.

1. Die Applikation soll eine Web-Oberfläche haben.

Folgende Funktionalitäten sollen realisiert werden:

* Daten für ein neues Objekt eingeben und Objekt speichern JA
* Alle vorhandenen Objekte in einer Tabelle anzeigen JA
* Ein bestehendes Objekt löschen JA
* Ein bestehendes Objekt aktualisieren JA
* SORTIEREN: Alle vorhandenen Objekte nach einem Attribut sortieren NOCH NICHT
---
#### Die Abgabe erfolgt nicht auf Moodle sondern auf GitHub Classroom (commit + push)